An actual readme coming soon, or never. i hate writing these

Helpful functions are in `Py`. They all have a C equivalent in `C`. To use that, build the C extensions with `build_C.sh` in `scripts`

The full list of valid solutions can be found at https://zenodo.org/records/3345889 since they're not mine and are also too large to upload here anyway
